git add .
git commit -m "Message"
git pull origin <branch name>

IF CONFILCTS OCCURES
git add .
git commit -m "Conflict fixed"

git push origin <branch name>

===== to be uploaded on git hub =====
crudMean
lazyloading

what is github?
GitHub ek web based hosting service hai jisme Git ki madad se version control karke ham apne project ke  programming code ko pratyek stage par.

what is git?
git is opensource version controlling tool.

git stash?
Now you want to switch branches, but you don’t want to commit what you’ve been working on yet,
so you’ll stash the changes. To push a new stash onto your stack, run git stash or git stash push:

hub stuff
rizwan@webcluesinfotech.com
rizvan1994 or rizwan1994

1.git init
2.git config user.name "RizvanShaikh"
3.git config user.email "rizwanshaikh8085@gmail.com"
4.git add *
5.git commit -m "some init msg"

git config user.name "wc.rizwan"
3.git config user.email "rizwan@webcluesifotech.com"

github & gist 
user name: RizvanShaikh
pw: rizvangithub1994
rizvan1994  linkedin pw
pw:rizwanskpe1994  userName:9879804646 skype
usrnm:8200120997 pw:rizvan1994 skype


If you want to rename the current branch, you can do:
git branch -m <newname>
